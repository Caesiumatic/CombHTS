# Run: 2026-06-19 — DFT calibration, gas-phase v1 (b3lyp/6-31G(d,p))
- run_id: 2026-06-19_dftcal-417442-gasphase
- date: 2026-06-19T20:05 (submitted) → 2026-06-21T21:25 (completed)
- command: `eps calibrate-dft --engine gaussian`
- engine / method: B3LYP/6-31G(d,p) **gas-phase opt-only (ΔE_SCF)** — neutral opt + cation opt, bare adiabatic IP
- scope: 22 monomers (xTB→DFT calibration set); 22/22 status ok
- cluster job: SGE **417442**, intel24 (256 GB), 72h wall, submitted 2026-06-19 20:05 — node reported (unverified)
- status: **completed** — exit 0; reported (unverified): the repo cannot confirm cluster state. ru_wallclock 177617 s (~49.3 h), maxvmem 1.49 GB; end 2026-06-21 21:25 local (`report.md` generated 2026-06-22T02:25:19Z). Cache: `b3lyp-6-31g(d,p)-smd` ×22, `gfn2-xtb` ×22 (22/22 ok).
- headline results:
  - **Fit-1 (xTB→DFT, all 22 rows, peak+onset):** slope 1.184748, intercept −0.619951 eV, **R² 0.9176, MAE 0.1415 eV**. Fit-1 never touches experiment, so label_type is irrelevant to it.
  - **Fit-2 (DFT→experiment, 18 peak rows; 4 onset EXCLUDED):** slope 0.554292 V/eV, intercept −2.375996 V, **R² 0.8541, MAE 0.1193 V**. Units differ (V vs eV) → a correlation fit, slope ≠ 1 expected.
  - **Composed xTB→DFT→exp line** `E = Fit2(Fit1(x))`: **slope 0.656696, intercept −2.719630 V** (verified from the two stage coefficients).
  - **Compose-vs-pinned:** over the xTB descriptor range present in the cal set (5.4541 .. 7.4123) the composed line and the pinned screening default (slope 0.725837, intercept −3.145372) agree to **max |Δ| = 0.0868 V** (at the top end, xTB≈7.412 = thiophene; +0.049 V at the bottom end). Within the ~0.1 V reference floor. INTERPOLATION-ONLY — extrapolation beyond this range may diverge (the slopes differ, 0.657 vs 0.726).
  - **FLAG — thiophene / core monomer:** core-monomer DFT→exp MAE = 0.292 V > 0.15 V. Thiophene is the single core monomer. Diagnosis (this run): its Fit-1 residual is **+0.350 eV (~2.5× the Fit-1 MAE)** and its Fit-2 residual is **−0.292 V** — i.e. its gas-phase ΔSCF DFT point sits HIGH at the extreme (highest-leverage) end of the descriptor range, and ~0.19 V of the 0.29 V Fit-2 miss propagates from that high DFT point through the Fit-2 slope. The benchmark row that fed Fit-2 (Cihaner & Önal 2007, ACN/TBABF₄, "first anodic oxidation value", tier A) is **natively Ag/AgCl with conversion = 0.0 V (identity)** — NO Pavlishchuk & Addison constant is applied to it. So the residual is NOT a misapplied reference-conversion constant; it is consistent with a **genuine method/outlier effect** (gas-phase ΔSCF overshoot on the smallest, highest-IP monomer), or the peak/onset literature spread (thiophene onset rows read 1.5–1.55 V vs the 2.05 V peak). The report's "re-examine the reference-conversion constant BEFORE re-tuning DFT" resolves cleanly: the constant is identity, so the next lever is the DFT point, not the reference.
- per-property failures: n/a (calibration batch, not a Tier-1 harvest); 0 skipped monomers.
- output artifacts (paths, NOT committed): `outputs/dft_calibration/{report.md, dft_calibration_points.csv}` and `outputs/dft_calibration_cache.sqlite` (cluster-only; not present on this checkout). Note `xtb_to_dft_calibration.json` and `*.provenance.json` were NOT found on disk at read time.
- **LABEL ARTIFACT (cache-key vs config mismatch):** the SQLite cache stores the DFT method string as `b3lyp-6-31g(d,p)-smd` (×22), but `report.md` Method/config states **gas-phase, opt only (ΔE_SCF)**. The `-smd` suffix is a STALE label inherited from the pre-T13 static cache string (`GAUSSIAN_METHOD_LABEL`); the actual calculation is gas-phase. This is a labeling artifact only — it does not change the numbers — and is exactly the config-blind cache-key class fixed in T13 (`Tier2Config.cache_method_label()` → `b3lyp/6-31g(d,p)/gas/freq:off`); this batch predates / did not pick up the new label string. Treat the cache `method` column as cosmetically stale, not as evidence of SMD.
- provenance: repo at git commit `82064cf` (2026-06-20) at the time of recording; `configs/tier2.yaml` = `smd_solvent: null`, `use_freq: false` (gas-phase v1); cache resumes across resubmits. **Actual submitted script config = committed `scripts/run_dft_calibration.sge` at d6f986d (h_rt 72:00:00 + `PYTHONUNBUFFERED=1`, `-q intel24 -pe smp 8`) + `configs/tier2.yaml` gas-phase v1** — verified 2026-06-21: `git stash list` empty and `refs/stash` absent, so there was NO uncommitted stash delta; the run's full script configuration lives in committed history (no reconcile needed).
- caveats: gas-phase v1; SMD+Freq deferred (the near-constant solvation offset is absorbed by the DFT→experiment fit intercept, so it does not change ranking/slope). The composed line is NOT pinned by this run — the pinned `configs/tier1.yaml` default and `default_screening_profile` are UNCHANGED; the keep-vs-adopt decision is presented to the PI (Friday). Honest MAE band stands (never claim < ~0.05 V precision or over-tune below ~0.1–0.15 V).
- supersedes / superseded_by: supersedes 2026-06-18_dftcal-417297-smd-freq
