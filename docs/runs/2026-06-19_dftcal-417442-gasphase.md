# Run: 2026-06-19 — DFT calibration, gas-phase v1 (b3lyp/6-31G(d,p))
- run_id: 2026-06-19_dftcal-417442-gasphase
- date: 2026-06-19T20:05 (submitted)
- command: `eps calibrate-dft --engine gaussian`
- engine / method: B3LYP/6-31G(d,p) **gas-phase opt-only (ΔE_SCF)** — neutral opt + cation opt, bare adiabatic IP
- scope: ~22 monomers (xTB→DFT calibration set)
- cluster job: SGE **417442**, intel24 (256 GB), 72h wall, submitted 2026-06-19 20:05 — node (e.g. compute-3-7) reported (unverified)
- status: **running** — reported (unverified): the repo cannot confirm cluster state
- headline results: pending. On completion → fit Fit-2 (DFT→experiment) on the strict & relaxed eligible sets, compare LOO-CV MAE, pick the anchor, compose the xTB→DFT→experiment calibration, drop/recompute the stale gas-phase thiophene point, then flip `configs/tier1.yaml` and reconcile it with `configs/calibration_profiles.yaml`. (slope/intercept/R²/LOO-CV: not pinned until the batch lands)
- per-property failures: n/a (calibration batch, not a Tier-1 harvest)
- output artifacts (paths, NOT committed): `outputs/dft_calibration/{dft_calibration_points.csv, report.md, xtb_to_dft_calibration.json}` (cluster-only; not present on this checkout)
- provenance: git commit not pinned in repo; `configs/tier2.yaml` = `smd_solvent: null`, `use_freq: false` (gas-phase v1); cache resumes across resubmits
- caveats: gas-phase v1; SMD+Freq deferred (the near-constant solvation offset is absorbed by the DFT→experiment fit intercept, so it does not change ranking/slope). Does NOT change the pinned default until the flip.
- supersedes / superseded_by: supersedes 2026-06-18_dftcal-417297-smd-freq
