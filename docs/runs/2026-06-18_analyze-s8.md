# Run: 2026-06-18 — eps analyze, directive §8 (read-only)
- run_id: 2026-06-18_analyze-s8
- date: 2026-06-18
- command: `eps analyze` (on the real-xTB Tier-1 harvest)
- engine / method: n/a — read-only post-processing of an existing harvest CSV (no engine call)
- scope: the real-xTB Tier-1 harvest (2026-06-16 run, 1,650 triads / 15×11×10)
- cluster job: cluster-side post-processing — reported (unverified)
- status: completed — reported (unverified): `outputs/analysis/` is NOT present on this checkout (artifacts are cluster-only / regenerated on demand, never committed)
- headline results: produced `summary.csv`, `shortlist.csv`, distribution PNGs (window margin, anion stability, solubility), `pareto_window_vs_solubility.png`, and a provenance sidecar. The Morgan-fingerprint t-SNE chemical-space map was SKIPPED (scikit-learn absent at run time); sklearn 1.9.0 was later installed via conda-forge (numpy<2 preserved), so the map is available on re-run.
- per-property failures: n/a (analyze consumes the harvest's failure columns; the underlying real-xTB harvest had 0 failures)
- output artifacts (paths, NOT committed): `outputs/analysis/summary.csv`, `outputs/analysis/shortlist.csv`, `outputs/analysis/*.png`, `outputs/analysis/*.provenance.json`
- provenance: git commit not pinned in repo; `eps analyze` is wired and test-covered (matplotlib/sklearn `importorskip`-gated)
- caveats: **screening-grade**; the `shortlist.csv` carries the screening-grade diagnostic note; the composite is **not a recommendation**. Any axis touching `optical_gap`/`dimerization_dG`/`band_gap_deviation`/`composite_score`/`pareto_front` is screening-grade (real but uncalibrated/proton-referenced).
- supersedes / superseded_by: —
