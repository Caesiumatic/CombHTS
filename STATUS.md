# Project Status
_Last updated: 2026-06-18_

## Current phase
Step-1 (Tier-1 xTB screen) is complete and clean: strict benchmark v3 is integrated (32 calibration-eligible raw rows, 32 collapsed groups under canonical SMILES + `solvent_name + label_type`; calibration profile-driven by `reference_frame`, `label_type`, tier, and medium), and the first fully-clean real-xTB harvest ran with zero calculation failures. Tier-1 xTB smoke auditability and per-property failure capture are verified on Lop with real xTB.

Step-2 (DFT calibration) is now in its FIRST LIVE RUN: the live g16 DFT-calibration batch is RUNNING on Lop (B3LYP/6-31G(d,p), targeting SMD(acetonitrile)). The gas-phase thiophene smoke ran OK (`dft_Eox` ≈ 8.51 eV); the solvated+freq full batch is slow on the big benchmark molecules (α-sexithiophene, di-aryl D-A, thienopyrrole-diones) and is running overnight — which is what drove the opt-only/gas-phase v1 fallback. **Known bug found this session (THINK T13): the DFT cache key is config-blind** — it omits the SMD solvent, the Freq toggle, and the basis/functional, so editing `configs/tier2.yaml` does not force a recompute. Until fixed, **wipe `outputs/dft_calibration_cache.sqlite` on any `tier2.yaml` change**; right now thiophene's "solvated" point is a stale gas-phase value (8.51 eV) reused from the gas-phase smoke. The Tier-2 Gaussian engine is config-driven (`configs/tier2.yaml`); `eps calibrate-dft` also emits the screen-ready DFT-anchored composed xTB→V calibration (`composed_xtb_to_AgAgCl_V`); `eps doctor` reports g16/Tier-2 readiness. The xTB->DFT (and composed) calibration is a NEW artifact; the pinned screening default is UNCHANGED pending the completed batch + PI sign-off. Once the batch lands, the strict-vs-relaxed screening anchor (T1) will be decided EMPIRICALLY by comparing Fit-2 LOO-CV MAE on both eligible sets (the n=9 strict fit's +37% in-sample→LOO jump now leans toward relaxed). Three literature reviews were added under `docs/research/` (new-monomer Eox; neutral-state polymer optical gaps; solvent windows); benzonitrile's provisional ESW fallback now has a research5 cross-check to fold in (Pt, 0.1 M TBAP: anodic ≈ +1.8 V, cathodic ≈ −1.95 V vs SCE).

An additive, reported-only oligomer Eox-vs-chain-length descriptor (with 1/n infinite-chain extrapolation) is now joined into the Tier-1 harvest; it does NOT enter any hard filter or the composite score (survivor counts and composite scores are unchanged).

The chemical-space library has been scaled toward directive §2: **36 monomers × 13 solvents × 15 salts = 7,020 triads** (was 15 × 11 × 10 = 1,650). Every new SMILES was RDKit-parsed + formula-checked, and every new monomer carries a verified coupling site (auto-derived α for the 5-membered heteroaromatics; explicit otherwise; D-A/fused-ring flagged approximate). The validated seed rows are byte-identical (purely additive). Species whose SMILES or coupling site could not be assigned with confidence were parked in `data/needs_review.md` rather than guessed into the live CSVs.

## What works and is verified
- Per-species architecture, SQLite caching, Engine abstraction, and mock-first tests.
- Redox -> V vs Ag/AgCl conversion is a single tested function with pinned constants.
- Benchmark validation enforces explicit label ontology before calibration: `label_type`, `calibration_eligible`, exclusion reason, reported/converted references, conversion method, source reference/locator/confidence, `medium_class`, and `reference_frame`.
- `configs/calibration_profiles.yaml` defines independent Ag/AgCl peak, Ag/AgCl onset, and placeholder Fc/Fc+ profiles. Profiles never pool different reference frames or co-fit peak and onset labels.
- `eps validate` runs the default screening profile; `eps validate --all-profiles` writes a profile comparison CSV and skips profiles with fewer than two collapsed points.
- Strict benchmark v3 has 32 rows, 32 calibration-eligible rows, and 32 collapsed groups: 19 `monomer_oxidation_peak`, 13 `monomer_oxidation_onset`, all `nonaqueous`, all `reference_frame=agagcl`.
- Profile point counts: `agagcl_peak_relaxed` = 19, `agagcl_onset_relaxed` = 13, `agagcl_peak_strict` = 9. The Fc/Fc+ profiles remain empty and skipped pending PI approval.
- New v3 sources: Cakal/Cihaner/Onal 2020 (10.1016/j.jelechem.2020.114000) FTPF/TTPT/STPS peak+onset rows in DCM; Oguzturk/Tirkes/Onal 2015 (10.1016/j.jelechem.2015.04.041) carbazole M1-M4 peak rows in MeCN from the journal, resolving the former M3 thesis conflict with published value 0.98 V; Algi et al. 2017 (10.1007/s10895-016-1978-x) pyridazinedione compounds 5/6 peak rows in MeCN.
- Asil/Cihaner/Onal 2009 TTT-Lum remains excluded in `data/benchmark_candidates.csv` because it was measured in 0.1 M LiClO4/MeCN + 5% BF3-Et2O, a Lewis-acid-modified medium rather than clean acetonitrile.
- `configs/tier1.yaml` monomer Eox calibration now comes from a real GFN2-xTB `eps validate --all-profiles` run on strict benchmark v3, using `agagcl_peak_strict`: slope 0.725837, intercept -3.145372, R^2 0.889, LOO-CV MAE 0.197 V.
- SeSeSe (DCM) hit a GFN2-xTB SCF non-convergence and was dropped from the `agagcl_peak_relaxed` fit; it is tier B and does not affect the chosen tier-A strict anchor.
- OSeO and FSeF share the same canonical SMILES but remain distinct calibration groups because their solvents differ.
- xTB Tier-1 smoke completed on Lop through Grid Engine/qsub using real xTB: 1650 attempted triads, 1273 ranked survivors, and 152 calculation-failure audit rows captured instead of aborting.
- The 152 real-xTB failures are now explained AND resolved (THINK T10 decided): 110 were EDOS-only `monomer_eox`/`dimerization` failures from RDKit force-field geometry corruption (MMFF/UFF cannot type Se, so UFF collapsed the geometry into a ~0.26 Å atom clash and xTB aborted geometry optimization) — fixed in `src/eps/structures/geometry.py` by skipping FF pre-optimization when no classical FF can type every atom and handing the clean ETKDG geometry (~1.0 Å min distance) to xTB. The remaining anion failures are gone: the harvest with `--iterations 500 --etemp 400` shows 0 `anion_eox` failures, so no ddCOSMO move is needed.
- First fully-clean real-xTB Tier-1 harvest completed on the SCS Lop cluster (GFN2-xTB, EDOS/Se geometry fix in place, cache rebuilt from scratch so every value reflects the fixed geometry path): 1650 triads (15 monomers × 11 solvents × 10 salts), ZERO calculation failures across all seven per-property stages (`monomer_Eox`, `solvation`, `optical_gap`, `dimerization`, `solvent_anodic_limit`, `solvent_cathodic_limit`, `anion_Eox`), and 1007 surviving triads. EDOS now computes (calibrated Eox ~1.47 V). This is the first complete, reproducible real-physics per-species descriptor harvest, produced with the shared oxidation calibration (T11). It is an engineering + real-physics milestone, not a final scientific ranking (see Scientific caution).
- Every primary CLI output (`run-tier1`/`validate`/`memo`/`analyze`) writes a
  `<output>.provenance.json` sidecar (timestamp, git commit+dirty, eps version, engine/method,
  config SHA-256, library sizes) — best-effort, never crashes the command. CI (`pytest`+`ruff`)
  runs on push/PR; `ruff check` is clean.
- The Tier-1 all-triads CSV now carries the scoring columns (composite_score/pareto_front/band_gap_deviation_eV/norm_*) joined from the ranked survivors by triad identity (survivors identical, non-survivors NaN/False), so `eps analyze` produces every §8 output (incl. Pareto + shortlist) from the single all-triads file.
- `eps analyze` (directive §8, read-only) turns a Tier-1 harvest CSV into `summary.csv` (total/surviving/retention overall + by monomer/solvent/salt_class + per-property failure counts), real-axis distribution PNGs, a Pareto PNG, a Morgan-fingerprint+descriptor chemical-space map (PCA→t-SNE, PCA(2) fallback for n<10), and a SCREENING-GRADE `shortlist.csv`. Any output touching `optical_gap`/`dimerization_dG`/`band_gap_deviation_eV`/`composite_score`/`pareto_front` is labeled screening-grade (real but uncalibrated/proton-referenced; not validated). Degrades gracefully if matplotlib/scikit-learn are absent or if the harvest lacks scoring columns (the all-triads audit currently does — point analyze at a scored CSV for the Pareto/shortlist). `matplotlib`+`scikit-learn` added as deps.
- Directive §4.3 Tier-3 is SCAFFOLDED and explicitly OPTIONAL / NOT validated (`src/eps/workflow/tier3.py`, `configs/tier3.yaml`, `eps tier3`). Method (a) range-separated hybrid DFT (CAM-B3LYP / 6-311+G(d,p) / SMD / opt+freq) is a REAL config option on the existing Gaussian engine (`write_tier3_dft_inputs` builds the .gjf inputs; g16 never runs in tests). Methods (b) explicit-solvation shell, (c) GFN2-xTB BOMD radical stability, and (d) Au(111)/ITO slab adsorption are DOCUMENTED HOOKS returning a `tier3_optional_not_run` flag until wired. No Tier-3 result feeds the ranking; the tier is not validated.
- Directive §4.2 Tier-2 refined screen (`src/eps/workflow/tier2.py` `run_tier2_refined_screen`; `eps tier2-screen`): the Tier-1 ranked survivors are re-filtered with the TIGHTER window constraint (monomer adiabatic IP ≥ 0.5 V below the solvent anodic limit, vs the Tier-1 0.3 V gate), keeping the anion-stability + solubility constraints, then re-ranked with the SAME §5 composite (scoring weights/formula unchanged). Mock-first: uses Tier-2 DFT Eox when a results CSV is supplied, else falls back to the calibrated Tier-1 Eox with `tier2_dft_pending=true`. Output `outputs/tier2_refined.csv`.
- Directive §4.1 MMFF94 conformer search is available in `src/eps/structures/geometry.py` as a config toggle (`conformer_search.enabled`, default on, n_conformers=100): embed N ETKDGv3 conformers, MMFF94-optimize, hand the lowest-energy one to xTB. This CHANGES geometries — it is NOT additive; enabling requires a FRESH Tier-1 harvest + re-validation (no byte-identical claim). The T10 Se-skip is preserved (non-MMFF-typable atoms fall back to the hardened single ETKDG embed). The conformer setting is folded into the engine cache-key `method` (so a single-conformer cache is never silently reused — same fix as the config-blind DFT cache, T13); `run_tier1` scopes it onto geometry generation via `conformer_search_active`. NOTE: with the directive default (enabled), the next real-xTB harvest's geometries/scores differ from the prior single-conformer harvest and must be re-validated.
- Directive §4.2 oligomer band-gap convergence check joined into the harvest (`src/eps/properties/optical_convergence.py`): the optical gap at n=1..6, the last-step delta |gap(n_max)−gap(n_max−1)|, and a `optical_gap_converged` flag (threshold default 0.1 eV). Purely ADDITIVE / reported-only (verified Δ=0); reuses the existing oligomer optical-gap path (n=6 shares the main `optical_gap_eV` cache key). Knob `bandgap_convergence` (default on, lengths 1..6).
- Directive §3 secondary descriptors are joined into the Tier-1 harvest (`src/eps/properties/secondary_descriptors.py`), purely ADDITIVE and REPORTED-ONLY (none enters a hard filter or the composite — verified survivors/composite identical with the section on vs off, Δ=0). Per-species, cached, failure-tolerant, screening-grade. §3.1 monomer: HOMO/LUMO/gap, radical-cation Mulliken spin (max + α-coupling-atom membership + α-spin sum), vertical IP + reorganization λ_ox. §3.2 solvent: λ_ox/λ_red reorganization. §3.3 electrolyte: anion vdW volume (RDKit 3D grid, Bondi additive fallback flagged for PF6⁻), cation reduction (RAW V — a reduction potential, NOT on the oxidation calibration per T11) + a reported below-cathodic flag, ion-pair dissociation ΔG (ALPB contact-pair proxy, skipped if unassemblable). Engine gained `homo/lumo/vertical_ip/vertical_ea` quantities + a per-atom `spin_density` array (MockEngine full + plausible; XTBEngine best-effort real paths flagged as screening approximations). Knob `secondary_descriptors.enabled` (default on); artifact `outputs/secondary_descriptors.csv`.
- Chemical-space library scaled toward directive §2 (purely additive; the 15/11/10 validated seed rows are byte-identical): `data/monomers.csv` 15→36, `data/solvents.csv` 11→13, `data/electrolytes.csv` 10→15, with a matching `data/polymerization.csv` coupling spec for every new monomer. New monomers: substituted thiophenes (3-Me, 3-F, 3,4-diF, 3-OMe), terthiophene, N-substituted/3,4-dimethyl pyrroles, 3-alkyl/3,4-dimethyl furans, bifuran/terfuran, 3-hexylselenophene, o-anisidine/o-toluidine/diphenylamine, N-vinylcarbazole, 9,9-dimethylfluorene, thieno[3,4-b]pyrazine, and dithieno[3,2-b:2',3'-d]pyrrole. New solvents: nitrobenzene, benzonitrile (ESW is a literature-PROVISIONAL fallback — the computed adiabatic anodic limit is primary — pending cross-check vs directive table 2.2). New salts: TEAPF6, LiBF4, KCl, CSA, HClO4. Every new SMILES is RDKit-parsed + formula-verified and every new coupling site assembles (n=2/n=6); auto-α for 5-membered heteroaromatics, explicit otherwise, with D-A/fused-ring couplings flagged approximate. Uncertain species (DPP, isoindigo, thiadiazoloquinoxaline, indacenodithiophene, o-aminophenol, 3,6-dimethylcarbazole; ionic liquids / DES / BFEE; NaPSS / NaDBSA) are deferred to `data/needs_review.md` with a one-line reason, not guessed into the live CSVs.
- Validation metrics extended (directive §7): `eps validate` now also reports Spearman ρ, residual std, the worst-5 predicted monomers, and MAE-by-chemical-family (RDKit substructure, no scipy); new `eps sanity` runs directional monomer-Eox checks within one solvent on the Tier-1 harvest; new `eps memo` writes `docs/validation_memo_<date>.md`. The §7 solvent-ESW MAE is now WIRED (`src/eps/validation/solvent_benchmark.py`; reported in `eps validate` + the memo §4): mean |computed anodic limit − measured anodic| (and cathodic) over `data/solvent_benchmark.csv`, comparing the RAW computed adiabatic ΔSCF limits. The benchmark is seeded HEADER-ONLY, so with zero rows it stays "not computable yet" (no engine calls, never fabricated) and becomes a real number the moment measured rows are curated in. The remaining §7 gap (yes/no feasibility >85% → no binary labels) is still marked not-computable. Real numbers come from the cluster xTB run.
- Tier-2 Gaussian engine is CONFIG-DRIVEN (no live g16 run performed): `configs/tier2.yaml` (v1 = B3LYP/6-31G(d,p), gas-phase ΔSCF, opt only) parameterizes SMD solvent, the Freq/thermal toggle, and Link0 `%mem`/`%nprocshared`. `build_gaussian_input` prepends the Link0 lines and adds `Freq` when `use_freq`; `GaussianEngine` reads `smd_solvent`/`use_freq` from the config. Setting `smd_solvent` + `use_freq: true` upgrades Eox to a solvated ΔG (documented rigor toggle). The return-code-before-parse safety and the "g16 absent → RuntimeError, never fabricate" behavior are unchanged; no Gaussian job is launched in the test suite (live test skips without g16).
- `eps calibrate-dft` (directive §7) builds the two-stage calibration mock-first: it REUSES the identical xTB descriptor as the xTB→experiment calibration (`monomer_eox_vs_AgAgCl`), so the new xTB→DFT slope/intercept are directly comparable. Calibration set = `data/benchmark.csv` rows with `calibration_eligible == true`, deduped by canonical SMILES (32 rows → 22 unique monomers). DFT Eox is cached per species (gas-phase v1; a cache hit reuses the neutral AND cation jobs); per-monomer failures are captured (`dft_calc_status`/`dft_calc_error`) and skipped. Writes `outputs/dft_calibration/{dft_calibration_points.csv, report.md, xtb_to_dft_calibration.json}` with both fits and a clearly-labeled side-by-side of the pinned xTB→exp vs new xTB→DFT slope/intercept (no files overwritten). It also emits the SCREEN-READY COMPOSED calibration (`composed_xtb_to_AgAgCl_V` in the JSON; a "Screen-ready calibration (DFT-anchored, directive §7)" section in the report): Fit 1 ∘ Fit 2 collapsed into one xTB-descriptor → V vs Ag/AgCl map (`composed_slope = fit2.slope·fit1.slope`, `composed_intercept = fit2.slope·fit1.intercept + fit2.intercept`, `mae_V = fit2.mae`), which has the SAME input/output as the pinned `monomer_eox` calibration and is therefore a drop-in replacement (adopt only after a live `--engine gaussian` batch + review). NEW artifact only: `default_screening_profile` and `configs/tier1.yaml` are UNCHANGED. `scripts/run_dft_calibration.sge` is the live-g16 template (node-local `GAUSS_SCRDIR`); it runs the SMALL calibration batch, distinct from the full Tier-2 production screen.
- `eps doctor` reports Tier-2 readiness without running g16: `tier2:g16` (g16 on PATH; WARN if absent, cluster-only) and `tier2:config` (configs/tier2.yaml loads, with the effective method string, e.g. "B3LYP/6-31G(d,p), gas phase, opt only (ΔE_SCF)").
- Additive oligomer Eox-vs-chain-length descriptor joined into the Tier-1 harvest (reported-only): RAW xTB adiabatic IE (eV) of the n-mer for `oligomer.eox_oligomer_lengths` (default [2,3,4,6]), reusing the existing oligomer assembly + side-chain truncation and the same neutral→cation convention; per-monomer, cached, failure-tolerant. New columns `oligomer_Eox_raw_n{1,2,3,4,6}`, `oligomer_Eox_infinite_raw_eV` (classic 1/n extrapolation incl. the n=1 monomer anchor), `oligomer_Eox_extrap_r2`, `oligomer_Eox_infinite_calibrated_V_vs_AgAgCl` (flagged `oligomer_Eox_calibration_out_of_domain = True`), `oligomer_Eox_sidechain_truncated`, `oligomer_Eox_calc_status`/`_error`. Verification artifact `outputs/oligomer_eox_series.csv`. Verified additive: the mock screen gives 113 → 113 survivors (identical sets), composite_score max abs diff 0.0.

## Scientific caution
- This is an engineering/pipeline milestone, not a final scientific ranking.
- Strict benchmark v3 meets the professor's original >=30 clean-group target under strict native-Ag/AgCl rules.
- The default screening profile remains provisional and needs PI sign-off on peak vs onset anchoring.
- The composite ranking is now SCREENING-GRADE (all five axes real physics), NOT placeholder-contaminated and NOT validated. Remaining honesty caveats: `optical_gap` is the sTDA-xTB (or HOMO-LUMO proxy) oligomer gap, UNCALIBRATED vs TD-DFT (Step-2); `dimerization_dG`'s ABSOLUTE value is set up to a fixed proton constant (relative ordering is sound); the anion/solvent oxidation values are monomer-fit calibration extrapolations (T11); and two coupling regiochemistries remain approximate (aniline N-para, the D-A simplification). A produced value is not a validated value.
- Keep label ontology strict: monomer oxidation potential, electropolymerization growth/onset potential, polymer doping onset, irreversible polymer-film redox, and ambiguous reference-electrode values must not be mixed.

## Placeholders / not yet validated
- optical_gap is now REAL (no longer a placeholder): the sTDA-xTB lowest singlet excitation of the assembled n=6 oligomer, or the oligomer GFN2-xTB HOMO-LUMO gap as a clearly-labeled proxy (`optical_gap_method = homo_lumo_hexamer_fallback`) when `stda` is absent. Screening-grade, UNCALIBRATED vs TD-DFT (Step-2 hook). For the OPTICAL-GAP oligomer only, long inert saturated side chains are truncated to methyl (electronically innocent; flagged `optical_gap_sidechain_truncated`) and embedding is hardened with random-coordinate retries — this fixed the dioctylfluorene-hexamer embed failure that had dropped the whole fluorene column. Open items: confirm `stda` availability on Lop; calibrate vs a TD-DFT reference.
- dimerization_dG is now REAL (no longer a placeholder): the xTB oxidative coupling ΔG `2 M+. -> M-M(neutral) + 2 H+` from the NEUTRAL α,α′-coupled dimer and the monomer radical cation (corrected from the earlier charge-imbalanced +2 dication, which made every monomer look ~+650 kcal/mol endothermic). The reaction is charge/electron-balanced so the bare proton's electronic energy is rigorously 0; ΔG = E(M-M neutral) − 2·E(M+.) is physically interpretable (dG<0 favorable), screening-grade (no thermal/ZPE/solvation; DFT-grade is Step-2). NOT a hard filter. The prior harvest's dimerization_dG values (and fluorene optical_gap) are SUPERSEDED pending the next cluster run.
- Oligomer assembly uses RDKit α-coupling (documented substitution for `stk`, which is unavailable in the env). Coupling regiochemistry is in `data/polymerization.csv` (human-reviewable; `eps run-tier1` also writes `outputs/oligomer_buildingblocks.csv`). APPROXIMATE coupling is flagged for the aniline family (N-para; o-anisidine/o-toluidine), diphenylamine (4,4'), the benzothiadiazole-thiophene D-A, and the new D-A/fused units thieno[3,4-b]pyrazine and dithieno[3,2-b:2',3'-d]pyrrole. RESOLVED: the EDOT/ProDOT/EDOP/EDOS SMILES in `data/monomers.csv` were corrected 2,3->3,4-dioxy (directive §2.1; both α free), so those four are now clean `alpha` 2,5-coupling (a regression test guards it). The four monomers' previously harvested Eox/optical_gap/dimerization_dG are now STALE pending the next cluster run; the pinned calibration is unaffected (none appear in `data/benchmark.csv`).
- Solvent anodic/cathodic limits are COMPUTED per spec §3.2 (adiabatic ΔSCF oxidation/reduction of the solvent molecule in implicit self-solvent, projected to V vs Ag/AgCl through the pinned redox function). The ANODIC limit is now on the shared oxidation calibration (T11 decided), so it sits on the same calibrated scale as monomer Eox; absolute calibrated values are screening-grade extrapolations (fit on monomer data) pending a solvent benchmark. The CATHODIC limit (via EA) stays raw/informational and is NOT used in any Tier-1 filter. The stopgap CSV `esw_*_V` values are retained as an explicit per-solvent fallback when the calc fails (CSV values are never calibrated).
- Monomer Eox calibration in `configs/tier1.yaml` is refit from real xTB on strict v3 (`agagcl_peak_strict`); it is still provisional and screening-grade. This single oxidation calibration is now shared by monomer Eox, solvent anodic limit, and anion Eox (T11).
- Fc/Fc+ profiles are empty placeholders until PI approval and clean native-Fc rows exist.
- Anion Eox is now on the shared oxidation calibration (T11), so the anion-stability filter is LIVE; absolute calibrated anion values are screening-grade extrapolations pending an anion benchmark.
- The window and anion-stability filters are now LIVE on one calibrated oxidation scale; previously they were effectively no-ops because the anion (and solvent) limits were raw while monomer Eox was calibrated (a raw-vs-calibrated scale mismatch).

## Open debts
1. (P0 science, MET) The >=30 clean experimental monomer oxidation group target is met by strict benchmark v3; future promotion still requires PI policy decision or source-level recovery.
2. DONE: `configs/tier1.yaml` now uses a real GFN2-xTB `eps validate --all-profiles` run on strict benchmark v3, profile `agagcl_peak_strict`.
3. Decide whether peak or onset should be the screening anchor; the current recommendation is `agagcl_peak_strict` pending PI sign-off.
4. Decide whether to fund a separate Fc/Fc+ track; do not force-convert incompatible Ag/Ag+, SCE, or polymer-onset rows to fill it.
5. Resolve or re-audit candidate/provenance rows in `data/benchmark_candidates.csv`, especially EDOT current rows, nonaqueous SCE/Ag/Ag+/pseudo-reference conversions, mixed solvents, Lewis-acid-modified media, and missing source locators.
6. Solvent anodic/cathodic limits are computed per spec §3.2 (adiabatic ΔSCF on the solvent molecule), with CSV as fallback; the anodic limit and anion Eox now share the monomer oxidation calibration (THINK T11 decided). Remaining future work is to validate the calibrated solvent/anion values against a measured solvent/anion benchmark (the calibration is currently a monomer-fit extrapolation).
7. ADDRESSED (fixture): `tests/fixtures/xtbout.json` is now schema-faithful (mirrors real `xtb --json` keys/nesting); still worth replacing with a captured real cluster dump when convenient, and keep checking full ALPB solvent availability for the proxy list.
8. Get professor sign-off on the 4 ALPB proxy solvents. (The ddCOSMO alternative is no longer needed on convergence grounds — T10 decided: 0 anion failures in the clean harvest — but PI sign-off on the ALPB proxies is still outstanding.)
9. DONE: `XTBEngine._run_xtb` now checks the subprocess return code and raises before parsing `xtbout.json`, so a garbage JSON cannot mask a real xTB failure (regression test added).
10. DONE (templates): version-controlled SGE templates live in `scripts/` (`run_tier1`, `run_validate`, `run_memo`, `run_analyze`) with a `#$ -S /bin/bash` first directive and the known-good module/conda/OMP preamble; see `scripts/README.md`. Submit via `qsub`, not interactively.
11. Upgrade placeholders: real oligomer assembly -> band gap; real dimer calculation.
12. PARTIAL: analysis/plots exist (`eps analyze`, directive §8); the Tier-2 DFT engine is now CONFIG-DRIVEN (`configs/tier2.yaml`; `GaussianEngine` reads SMD/Freq/Link0, fixture-tested, never run live) and the Step-2 two-stage calibration is BUILT mock-first (`eps calibrate-dft` writes the xTB→DFT + DFT→experiment artifacts; `scripts/run_dft_calibration.sge` is ready). Still not done: (a) the LIVE g16 calibration batch on Lop (run `run_dft_calibration.sge`; it is the SMALL ~32-monomer batch, distinct from the full Tier-2 screen); (b) deciding, after live numbers + PI sign-off, whether the DFT-anchored COMPOSED calibration (`composed_xtb_to_AgAgCl_V`, now emitted as a drop-in for the tier1.yaml `monomer_eox` slope/intercept) should ever replace the pinned xTB→experiment default (currently it does NOT); (c) running the full Tier-2 production screen at scale (PI/T8 decision), expanded libraries toward ~100×30×25 (IN PROGRESS: now 36×13×15 = 7,020 triads per directive §2; remaining named species are parked in `data/needs_review.md` pending verified SMILES + coupling assignments), HPC orchestration.
14. The additive oligomer Eox-vs-chain-length descriptor is REPORTED-ONLY (RAW trend + 1/n extrapolation are the durable signal; the calibrated infinite value is flagged out-of-domain). It is deliberately NOT wired into any filter or the composite score. Open question (see THINK T12): whether/how the chain-length-corrected Eox should ever inform ranking, and what oligomer reference would calibrate it.
13. Reconcile the calibration-anchor mismatch between `configs/tier1.yaml` (`agagcl_peak_strict`) and `configs/calibration_profiles.yaml` default screening profile (`agagcl_peak_relaxed`).

## Immediate next action
Two parallel asks: (1) get PI sign-off on using `agagcl_peak_strict` as the screening calibration anchor; (2) run the live g16 DFT-calibration batch on Lop (`qsub scripts/run_dft_calibration.sge`; verify the env first with `eps doctor`) to populate the xTB→DFT and DFT→experiment fits with real numbers — the engineering is built and tested mock-first, only the live run is pending. The xTB→DFT calibration remains a NEW artifact and does NOT change the pinned default.

## Architecture invariants
See AGENTS.md: compute per-species, not per-triad; mock-first; data in CSV and thresholds/profiles in YAML; SQLite cache; one pinned redox function.
